(function (PKM) {
  let initialized = false;
  const toggle = (key, checked, label) => `<div class="setting-control"><div class="setting-control__copy"><strong>${label[0]}</strong><p>${label[1]}</p></div><label class="switch"><span class="sr-only">${label[0]}</span><input type="checkbox" data-setting="${key}" ${checked ? 'checked' : ''}><span></span></label></div>`;
  function render() {
    const main = document.getElementById('page-content'); if (!main) return; const state = PKM.state.get(), settings = state.settings;
    main.innerHTML = `<div class="content-page settings-page"><header class="page-header"><div class="page-header__copy"><p class="eyebrow">Preferences and data safety</p><h1>Settings</h1><p>Control appearance, canvas behavior, study timing, navigation, backups, and accessibility.</p></div></header><div class="settings-grid"><section class="section-card settings-section"><div class="section-heading"><div><p class="eyebrow">Appearance</p><h2>Workspace style</h2></div></div><div class="settings-section__body"><div class="setting-control"><div class="setting-control__copy"><strong>Color mode</strong><p>Choose light, dark, or your system preference.</p></div><div class="segmented" role="group" aria-label="Color mode">${['light','dark','system'].map((mode) => `<button type="button" data-theme-choice="${mode}" class="${settings.theme === mode ? 'is-active' : ''}">${mode[0].toUpperCase()+mode.slice(1)}</button>`).join('')}</div></div>${toggle('compactDensity', settings.compactDensity, ['Compact density', 'Fit more information into lists and panels.'])}</div></section><section class="section-card settings-section"><div class="section-heading"><div><p class="eyebrow">Canvas</p><h2>Map behavior</h2></div></div><div class="settings-section__body">${toggle('gridEnabled', settings.gridEnabled, ['Grid', 'Show alignment points behind topics.'])}${toggle('minimapEnabled', settings.minimapEnabled, ['Minimap', 'Show the navigable map overview.'])}${toggle('connectionLabels', settings.connectionLabels, ['Connection labels', 'Keep relationship labels visible.'])}${toggle('autoFitOnOpen', settings.autoFitOnOpen, ['Auto-fit on open', 'Fit all topics when a map opens.'])}<div class="setting-control"><div class="setting-control__copy"><strong>Default zoom</strong><p>Starting scale for each map.</p></div><label class="field setting-select"><span class="sr-only">Default zoom</span><select data-setting="defaultZoom">${[.75,1,1.25,1.5].map((value) => `<option value="${value}" ${settings.defaultZoom === value ? 'selected' : ''}>${value * 100}%</option>`).join('')}</select></label></div></div></section><section class="section-card settings-section"><div class="section-heading"><div><p class="eyebrow">Study</p><h2>Review timing</h2></div></div><div class="settings-section__body"><div class="study-settings">${['Learning','Reviewing','Mastered'].map((status) => `<label class="field"><span>${status} interval</span><span class="number-suffix"><input type="number" min="1" max="365" data-review-setting="${status}" value="${settings.reviewIntervals[status]}"><i>days</i></span></label>`).join('')}<label class="field"><span>Default session size</span><select data-setting="sessionSize">${[3,5,10,15,20].map((value) => `<option value="${value}" ${settings.sessionSize === value ? 'selected' : ''}>${value} topics</option>`).join('')}</select></label></div></div></section><section class="section-card settings-section"><div class="section-heading"><div><p class="eyebrow">Navigation</p><h2>Workspace defaults</h2></div></div><div class="settings-section__body"><div class="setting-control"><div class="setting-control__copy"><strong>Default map</strong><p>The map selected as your starting subject.</p></div><label class="field setting-select"><span class="sr-only">Default map</span><select data-setting="defaultMapId"><option value="">Most recent map</option>${state.maps.map((map) => `<option value="${map.id}" ${settings.defaultMapId === map.id ? 'selected' : ''}>${PKM.helpers.escapeHtml(map.name)}</option>`).join('')}</select></label></div><div class="setting-control"><div class="setting-control__copy"><strong>Desktop sidebar</strong><p>Choose the default sidebar width.</p></div><label class="field setting-select"><span class="sr-only">Sidebar preference</span><select data-setting="sidebarCollapsed"><option value="false" ${!settings.sidebarCollapsed ? 'selected' : ''}>Expanded</option><option value="true" ${settings.sidebarCollapsed ? 'selected' : ''}>Collapsed</option></select></label></div>${toggle('restoreLastPage', settings.restoreLastPage, ['Restore last page', 'Return to the most recently opened page.'])}</div></section><section class="section-card settings-section"><div class="section-heading"><div><p class="eyebrow">Accessibility</p><h2>Comfort and contrast</h2></div></div><div class="settings-section__body">${toggle('reduceMotion', settings.reduceMotion, ['Reduce motion', 'Disable interface transitions and animated movement.'])}${toggle('highContrast', settings.highContrast, ['Increase contrast', 'Strengthen borders and text contrast.'])}${toggle('animations', settings.animations, ['Interface animation', 'Allow restrained interface transitions.'])}</div></section><section class="section-card settings-section settings-data"><div class="section-heading"><div><p class="eyebrow">Data</p><h2>Export, import, and reset</h2></div></div><div class="settings-section__body"><div class="data-action"><span class="data-action__icon">${PKM.helpers.icon('download')}</span><div><strong>Export knowledge data</strong><p>Choose everything, the current map, or a selected map. Metadata is included.</p><div class="inline-data-controls"><select data-export-type aria-label="Export type"><option value="all">Everything</option><option value="current-map">Current map</option><option value="selected-map">Selected map</option></select><select data-export-map aria-label="Map to export">${state.maps.map((map) => `<option value="${map.id}" ${map.id === state.activeMapId ? 'selected' : ''}>${PKM.helpers.escapeHtml(map.name)}</option>`).join('')}</select></div></div><button class="button button--secondary" type="button" data-export>${PKM.helpers.icon('download', 'Export')}</button></div><div class="data-action"><span class="data-action__icon">${PKM.helpers.icon('upload')}</span><div><strong>Import a backup</strong><p>Replace current data or safely merge imported maps with new IDs.</p><select data-import-mode aria-label="Import mode"><option value="replace">Replace existing data</option><option value="merge">Merge with existing data</option></select></div><button class="button button--secondary" type="button" data-import>${PKM.helpers.icon('upload', 'Choose File')}</button><input class="sr-only" type="file" accept="application/json,.json" data-import-file tabindex="-1" aria-hidden="true"></div><div class="data-action data-action--danger"><span class="data-action__icon">${PKM.helpers.icon('trash')}</span><div><strong>Reset application data</strong><p>Create a final snapshot, then remove maps, activity, and preferences.</p></div><button class="button button--ghost danger-text" type="button" data-reset-data>Reset</button></div></div></section><section class="section-card settings-section settings-data"><div class="section-heading"><div><p class="eyebrow">Backup</p><h2>Local snapshots</h2></div><button class="button button--secondary" type="button" data-create-backup>${PKM.helpers.icon('backup', 'Create Backup')}</button></div><div class="settings-section__body"><div class="setting-control"><div class="setting-control__copy"><strong>Automatic backups</strong><p>Create snapshots before imports, resets, and large destructive actions.</p></div><label class="switch"><span class="sr-only">Automatic backups</span><input type="checkbox" data-setting="automaticBackups" ${settings.automaticBackups ? 'checked' : ''}><span></span></label></div><div class="backup-list">${state.backups.length ? state.backups.map((backup) => `<article><span class="data-action__icon">${PKM.helpers.icon('backup')}</span><div><strong>${PKM.helpers.escapeHtml(backup.reason)}</strong><p>${PKM.helpers.formatDate(backup.createdAt)} · ${PKM.helpers.formatRelative(backup.createdAt)}</p></div><button class="button button--secondary" type="button" data-backup-action="restore" data-backup-id="${backup.id}">Restore</button><button class="icon-button danger-text" type="button" data-backup-action="delete" data-backup-id="${backup.id}" aria-label="Delete backup">${PKM.helpers.icon('trash')}</button></article>`).join('') : `<p class="backup-empty">No snapshots yet. Up to ${PKM.constants.BACKUP_LIMIT} recent backups are kept.</p>`}</div></div></section></div></div>`;
    main.querySelectorAll('[data-theme-choice]').forEach((button) => { button.setAttribute('aria-pressed', String(button.classList.contains('is-active'))); button.addEventListener('click', () => { PKM.theme.set(button.dataset.themeChoice); PKM.toast.show('Theme preference saved'); }); }); main.querySelectorAll('[data-setting]').forEach((control) => control.addEventListener('change', () => { const key = control.dataset.setting; let value = control.type === 'checkbox' ? control.checked : control.value; if (['defaultZoom','sessionSize'].includes(key)) value = Number(value); if (key === 'sidebarCollapsed') value = value === 'true'; PKM.state.update((draft) => { draft.settings[key] = value; if (key === 'defaultMapId' && value) draft.activeMapId = value; }, { source: 'settings' }); PKM.toast.show('Setting saved'); })); main.querySelectorAll('[data-review-setting]').forEach((control) => control.addEventListener('change', () => { PKM.state.update((state) => { state.settings.reviewIntervals[control.dataset.reviewSetting] = Number(control.value); }, { source: 'settings' }); PKM.toast.show('Review interval saved'); }));
    main.querySelector('[data-export]').addEventListener('click', () => { const type = main.querySelector('[data-export-type]').value, mapId = type === 'current-map' ? state.activeMapId : main.querySelector('[data-export-map]').value; PKM.exporter.download(type, mapId); }); const file = main.querySelector('[data-import-file]'); main.querySelector('[data-import]').addEventListener('click', () => file.click()); file.addEventListener('change', () => { PKM.importer.read(file.files[0], main.querySelector('[data-import-mode]').value); file.value = ''; });
    main.querySelector('[data-create-backup]').addEventListener('click', () => PKM.backups.create()); main.querySelectorAll('[data-backup-action]').forEach((button) => button.addEventListener('click', () => { const backup = state.backups.find((item) => item.id === button.dataset.backupId); if (button.dataset.backupAction === 'restore') PKM.modal.confirm({ title: 'Restore this backup?', message: `Your current data will be replaced by “${backup.reason}”.`, confirmLabel: 'Restore backup', danger: true, onConfirm: () => { PKM.backups.restore(backup.id); PKM.toast.show('Backup restored'); return true; } }); else PKM.modal.confirm({ title: 'Delete this backup?', message: 'This snapshot cannot be recovered after deletion.', confirmLabel: 'Delete backup', danger: true, onConfirm: () => { PKM.backups.remove(backup.id); PKM.toast.show('Backup deleted'); return true; } }); }));
    main.querySelector('[data-reset-data]').addEventListener('click', () => PKM.modal.confirm({ title: 'Reset all application data?', message: 'A final local snapshot will be kept. All maps, topics, connections, activity, and preferences will be reset.', confirmLabel: 'Reset everything', danger: true, onConfirm: () => { const current = PKM.helpers.clone(PKM.state.get()); PKM.backups.addToDraft(current, 'Before application reset'); const next = PKM.storage.blankState(); next.backups = current.backups; PKM.state.replace(next, { source: 'reset', historyLabel: 'Reset application data' }); PKM.theme.apply('light'); PKM.toast.show('Application data reset'); return true; } }));
  }
  function init() { if (initialized) return; initialized = true; render(); document.addEventListener('pkm:statechange', render); }
  PKM.pages = PKM.pages || {}; PKM.pages.settings = { init, render };
})(window.PKM = window.PKM || {});
